<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Scan REST controllers -->
    <context:component-scan base-package="com.university.portal.controller"/>

    <!-- Enable annotation-driven MVC (also registers Jackson MessageConverter) -->
    <mvc:annotation-driven/>

    <!-- Serve static resources (AngularJS SPA, CSS, JS) from webapp root -->
    <mvc:resources mapping="/app/**"       location="/app/"/>
    <mvc:resources mapping="/css/**"       location="/css/"/>
    <mvc:resources mapping="/fonts/**"     location="/fonts/"/>

    <!-- Default servlet handler for index.html at root -->
    <mvc:default-servlet-handler/>

</beans>
